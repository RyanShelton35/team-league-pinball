version: 1
applnications:
  - appRoot: pinball-app
    backend:
      phases:
        build:
          commands:
            - npm ci --cache .npm --prefer-offline
            - npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID --out-dir ./dist/
    frontend:
      phases:
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
          - './dist/amplify_outputs.json' # Include amplify_outputs.json
      cache:
        paths:
          - .npm/**/*
          - node_modules/**/*